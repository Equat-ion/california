# Packages

Shared code that is consumed by one or more apps belongs here.

Add reusable libraries, UI primitives, API clients, or cross-app utilities as separate workspace packages under this directory.